import LoginLayer from "@/components/LoginLayer";

export const metadata = {
  title: "Sign In | CV ANT",
  description: "Sign in admin, owner, atau customer.",
};

const Page = () => {
  return <LoginLayer />;
};

export default Page;
